# commit to be tagged for the new release
commit = "HEAD"

project_name = "dhcp-proxy-ipxe"
github_repo = "siderolabs/dhcp-proxy-ipxe"
match_deps = "^github.com/(siderolabs/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
